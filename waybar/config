{
    "layer": "top",
    "position": "top",
    "modules-right": ["network", "cpu", "memory", "clock"],

    "cpu": {
        "interval": 10,
        "format": "{}% ",
        "max-length": 10
    },

    "memory": {
        "interval": 35,
        "format": "{used:0.1f}G/{total:0.1f}G "
    },
    
    "clock": {
        "interval": 60,
        "format": "{:%d %b %H:%M}" ,
        "max-length": 25,
    },

    "network": {
        "tooltip-format": "{ifname} {bandwidthUpBits}󰊗",
        "max-length": 50
    }
}
